{
  "frontend/src/App.tsx": "Main application shell with header displaying \"LS Group Inventory Management System\", a 6-tab navigation (Inventory, Bardana, Orders, Tools & Machinery, Raw Materials, Change Log), and a footer with attribution. No authentication required — full admin access on load.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend interactions — fixes the invalid `ImmutableProduct` import (replaced with `ProductView` from backend), adds boolean return-value checks in `addInventoryStock`, `removeInventoryStock`, `setBardanaStock`, and `setRawMaterialStock` mutations so failures throw errors and trigger proper error toasts instead of silently succeeding.",
  "frontend/src/lib/utils.ts": "Utility functions including cn() for class merging, IST timestamp formatting, nanosecond/date conversions, and the sortProducts helper for descending-by-stock sorting with zero-stock items maintaining original order at bottom.",
  "frontend/src/components/InventoryTab.tsx": "Inventory tab with LS Pulses and LS Foods LLP sub-tabs — fixes plant key resolution with a 4-step matching strategy (exact → case-insensitive → normalized → positional fallback) to ensure the correct backend map key is always passed to add/remove stock mutations, and tightens quantity validation to reject empty inputs.",
  "frontend/src/components/BardanaTab.tsx": "Bardana tab with LS Pulses and LS Foods LLP sub-tabs — fixes the `ImmutableProduct` → `ProductView` type import, fixes plant lookup to match by map key, and improves error handling to surface backend error messages in toasts.",
  "frontend/src/components/OrdersTab.tsx": "Orders tab with a persistent table (Date, Bags, Brand, Rate, Party Name, Dalal Name, Remarks), inline add/edit/delete rows, auto-populated today's date, and auto-save on submit. Fixed by replacing the non-existent `ImmutableOrder` import with `OrderView` from backend, and destructuring the `[bigint, OrderView]` tuple returned by `useGetOrders` so all order fields are accessed correctly.",
  "frontend/src/components/ToolsMachineryTab.tsx": "Tools & Machinery tab with a persistent table (Product, Quantity, Remarks), inline add/edit/delete rows with auto-save.",
  "frontend/src/components/RawMaterialsTab.tsx": "Raw Materials tab with LS Pulses and LS Foods LLP sub-tabs — fixes the `ImmutableProduct` → `ProductView` type import, fixes plant lookup to match by map key, and improves error handling to surface backend error messages in toasts.",
  "frontend/src/components/ChangeLogTab.tsx": "Read-only Change Log tab displaying Inventory stock changes with IST timestamp, plant, product, quantity change (signed), and user; sorted newest-first with 3-day retention handled by backend.",
  "frontend/src/index.css": "Global styles with industrial slate+amber OKLCH color system, Inter/Rajdhani fonts, and custom header color tokens for both light and dark modes.",
  "frontend/tailwind.config.js": "Tailwind config extended with Inter/Rajdhani font families, industrial color palette tokens including header-specific colors, and standard shadcn/ui theme extensions."
}